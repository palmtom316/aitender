[project]
name = "aitender-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.116.1",
  "python-multipart==0.0.20",
  "uvicorn==0.35.0"
]

[dependency-groups]
dev = [
  "httpx==0.28.1",
  "pytest==8.3.5"
]
